使用UDP内网查询服务时，有以下四点注意事项：
1.若UDP SERVER配置有多个端口，客户端需要轮询或随意访问端口，以确保SERVER的每个网卡队列及网络I/O负载均衡；
2.使用预留字段reservedBuffer时，为二进制流数据，前四字节表示数据长度，接下来字节表示实际数据；
3.url长度最大值为1280，若超过长度则会截取；
4.若客户端采用异步请求，请确保uiSeq和请求包的一一对应关系。